@model IEnumerable<OpenSchool.Models.ProfesoresCursos>

@{
    ViewBag.Title = "Index";
}

<div class="text-center">
    <br />
    <p class="h1">Listado de Profesores Cursos</p>
</div>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cursos.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuarios.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Cursos.Nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuarios.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdProfCur }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdProfCur }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdProfCur })
        </td>
    </tr>
}

</table>
